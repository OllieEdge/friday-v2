# Friday v2 runtime (copy to .env on the host; do not commit .env)

# Runner selection:
# - noop: returns a helpful placeholder (default)
# - openai: calls OpenAI Responses API
FRIDAY_RUNNER=noop

# OpenAI (required when FRIDAY_RUNNER=openai)
OPENAI_API_KEY=__PASTE__
OPENAI_MODEL=gpt-4o-mini

