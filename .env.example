# Friday v2 runtime (copy to .env on the host; do not commit .env)

# Runner selection:
# - noop: no model calls (default)
# - codex: uses Codex CLI with the active profile from Settings → Accounts
# - openai: uses OpenAI API via OPENAI_API_KEY (metered fallback)
FRIDAY_RUNNER=noop

# Codex CLI (recommended runner)
# If Codex isn't on the PATH (launchd shells often have a minimal PATH), set an absolute path:
# CODEX_PATH=/Users/ollie/bin/codex
CODEX_PATH=
# Optional: force the Codex model (e.g. your preferred Codex model string)
CODEX_MODEL=

# Base URL (public) for OAuth callbacks and links
FRIDAY_BASE_URL=https://friday2.edgflix.com

# Optional admin gate for sensitive endpoints (OAuth connect/disconnect, etc).
# When set, the UI will need to send `x-friday-admin-token` (we can wire this later).
FRIDAY_ADMIN_TOKEN=

# Google OAuth (required to connect Google accounts)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Space-separated scopes. If you change scopes, you must re-connect accounts.
GOOGLE_SCOPES="openid email profile https://www.googleapis.com/auth/gmail.readonly"

# Metered fallback runner (OpenAI API)
# OPENAI_API_KEY=...
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
OPENAI_MODEL=
# OPENAI_TIMEOUT_MS=60000
OPENAI_TIMEOUT_MS=

# Optional metered cost estimates (USD per 1K tokens).
# Used for showing a running cost total when your Codex profile is authenticated via API key.
# METERED_USD_PER_1K_INPUT=
# METERED_USD_PER_1K_CACHED_INPUT=
# METERED_USD_PER_1K_OUTPUT=
