# Friday v2 runtime (copy to .env on the host; do not commit .env)

# Runner selection:
# - noop: no model calls (default)
# - codex: uses Codex CLI with the active profile from Settings → Accounts
# - openai: uses OpenAI API via OPENAI_API_KEY (metered fallback)
FRIDAY_RUNNER=noop

# Codex CLI (recommended runner)
# If Codex isn't on the PATH (launchd shells often have a minimal PATH), set an absolute path:
# CODEX_PATH=/Users/ollie/bin/codex
CODEX_PATH=
# Optional: force the Codex model (e.g. your preferred Codex model string)
CODEX_MODEL=

# Metered fallback runner (OpenAI API)
# OPENAI_API_KEY=...
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
OPENAI_MODEL=
# OPENAI_TIMEOUT_MS=60000
OPENAI_TIMEOUT_MS=
