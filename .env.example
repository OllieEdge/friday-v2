# Example environment file for Friday v2.
# Copy to `.env` (do not commit `.env`).

# Runner selection:
# - `settings` uses Settings → Accounts → Assistant runner.
# - `codex` uses the Codex CLI profile selected in Settings → Accounts.
# - `openai` / `metered` uses the OpenAI API key below.
# - `vertex` uses Google Vertex AI (Gemini) via the Vertex config below.
FRIDAY_RUNNER=settings

# Optional: force a Codex binary path (otherwise it will auto-discover).
CODEX_PATH=

# Model used by the runner (Codex CLI) when `FRIDAY_RUNNER=codex`.
CODEX_MODEL=gpt-5.2-2025-12-11

# Base URL for OAuth callbacks (Google, etc).
FRIDAY_BASE_URL=https://friday2.edgflix.com

# OpenAI API (metered) for `FRIDAY_RUNNER=openai` / `metered` / `api`.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.2-2025-12-11
OPENAI_BASE_URL=https://api.openai.com

# Google Vertex AI (Gemini) for `FRIDAY_RUNNER=vertex` (or select in UI).
# Non-secret settings can also be set in the UI (model/project/location).
VERTEX_MODEL=gemini-2.0-flash
VERTEX_PROJECT_ID=tmg-product-innovation-prod
VERTEX_LOCATION=europe-west2
VERTEX_MAX_OUTPUT_TOKENS=8192
VERTEX_CONTEXT_CACHE=1
VERTEX_CONTEXT_CACHE_TTL_S=3600
VERTEX_CODE_EXECUTION=
VERTEX_TOOL_EXEC=

# Optional: Vertex auth via Google OAuth (uses the "Google" account connected in Settings → Accounts).
# Requires `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` and that the connected account has granted
# `https://www.googleapis.com/auth/cloud-platform` (use "Enable Vertex" in the UI).
VERTEX_AUTH_MODE=
VERTEX_GOOGLE_ACCOUNT_KEY=work

# Auth options (choose one, keep secrets out of git):
# 1) Service account JSON file path (recommended).
VERTEX_SERVICE_ACCOUNT_FILE=
# 2) Inline JSON (not recommended).
VERTEX_SERVICE_ACCOUNT_JSON=
# 3) AWS Secrets Manager secret containing service account JSON.
VERTEX_AWS_SECRET_ID=gcp-tmg-product-innovation-prod-all-access
VERTEX_AWS_REGION=eu-west-1
VERTEX_AWS_PROFILE=telegraph
# If this uses AWS SSO, ensure the session is valid (e.g. `aws sso login --profile telegraph`).
# 4) Direct access token (debug only).
VERTEX_ACCESS_TOKEN=

# Host tool execution (HMAC-authenticated).
FRIDAY_TOOL_HMAC_SECRET=
FRIDAY_TOOL_HMAC_TTL_S=300
FRIDAY_TOOL_ALLOW_ALL=
FRIDAY_TOOL_TIMEOUT_MS=60000
FRIDAY_TOOL_MAX_OUTPUT_BYTES=200000

# Cost estimation (USD per 1K tokens).
# These are used to estimate cost for metered usage in the UI.
METERED_USD_PER_1K_INPUT=0.001751
METERED_USD_PER_1K_CACHED_INPUT=0.000175
METERED_USD_PER_1K_OUTPUT=0.013992

# Google OAuth (optional).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_SCOPES=openid email profile https://www.googleapis.com/auth/gmail.readonly https://www.googleapis.com/auth/gmail.send https://www.googleapis.com/auth/calendar https://www.googleapis.com/auth/chat.messages https://www.googleapis.com/auth/cloud-platform
